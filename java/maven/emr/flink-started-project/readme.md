### Flink starter project to run on EMR

It is possible to deploy environment specific property file by
passing argument `-env <environment>`. [environment can be dev, local, qa etc].

#Build project & create jar
`mvn clean package`